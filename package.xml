<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>marine_sonar_widgets</name>
  <version>0.1.0</version>
  <description>Shared Qt/OpenGL sonar-display widgets (scrolling backscatter
  waterfall, water-column echogram) and a framework-agnostic target-marking core
  for the UNH marine autonomy stack. One implementation feeds both the live
  rqt_operator_tools plugins and the offline marine_perception_tools viewer, and
  both produce the same marine_interfaces/msg/Contact when an analyst marks a
  target. No rqt / rclcpp / rosbag2 dependency. Scaffold only; widgets and tests
  arrive via the extraction issue.</description>
  <maintainer email="roland@ccom.unh.edu">Roland Arsenault</maintainer>
  <license>BSD-3-Clause</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <build_depend>qtbase5-dev</build_depend>
  <!-- The exported library links Qt5::OpenGL, so downstream builders need the
       Qt OpenGL dev package too (build_export), not just this package's own
       build. -->
  <build_depend>libqt5-opengl-dev</build_depend>
  <build_export_depend>libqt5-opengl-dev</build_export_depend>

  <depend>marine_colormap</depend>
  <depend>marine_acoustic_msgs</depend>

  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>
  <test_depend>ament_cmake_gtest</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
